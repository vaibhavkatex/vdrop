{
  "name": "vdrop",
  "version": "1.0.0",
  "description": "VDROP — Download Anything. No redirect, seedha yahan se.",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
